# Production Environment Configuration
# 
# IMPORTANT: Generate secure values using:
#   python scripts/python/generate_api_keys.py
#   or: make keys
#
# DO NOT use the example values below in production!

# Application
APP_ENV=production
DEBUG=false

# Redis Configuration
# Option 1: Single Redis instance (development/staging)
REDIS_URL=redis://redis:6379/0

# Option 2: Redis Cluster (production - recommended)
# Run: bash scripts/shell/setup_redis_cluster.sh
#   or: make redis-cluster
# REDIS_URL=redis://:YOUR_REDIS_PASSWORD@redis-node-1:6379
# REDIS_PASSWORD=GENERATE_SECURE_PASSWORD

# Option 3: Managed Redis (AWS ElastiCache, Azure Cache, etc.)
# REDIS_URL=rediss://your-elasticache.amazonaws.com:6379/0

# OpenAI Configuration
OPENAI_API_KEY=sk-prod-your-key-here

# Security - GENERATE SECURE KEYS!
# Run: python scripts/generate_api_keys.py
API_KEYS=REPLACE_WITH_GENERATED_KEYS
ENABLE_AUTH=true
ENABLE_RATE_LIMIT=true

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# API Settings
API_TITLE=RAG Pipeline API
API_VERSION=1.0.0

# RAG Configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
LLM_MODEL=gpt-4
EMBEDDING_MODEL=text-embedding-3-small
RETRIEVAL_TOP_K=5

# Monitoring
GRAFANA_PASSWORD=GENERATE_SECURE_PASSWORD

# Celery Configuration
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_TIMEZONE=UTC
CELERY_RESULT_EXPIRES=3600
